# Runtime dependencies for the soleire web application.
# Pinned to compatible ranges; regenerate a lockfile with `pip-compile` if you
# want exact pins for production.

Django>=5.2,<5.3
psycopg[binary]>=3.2,<4        # psycopg 3 — the supported PostgreSQL driver for Django 5.x
gunicorn>=23.0,<24
whitenoise[brotli]>=6.7,<7     # serves collected static files without a separate web server
argon2-cffi>=23.1              # Argon2 password hashing (first entry in PASSWORD_HASHERS)
